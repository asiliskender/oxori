# No Frontmatter File

This file has no YAML frontmatter — no `---` delimiters at all.

It is used to test that the parser returns an empty `frontmatter` object rather
than throwing or returning undefined.

The body contains tags: #project/beta and #status/pending for tag extraction tests.

And wikilinks: [[overview]] and [[note-one]] for wikilink extraction tests.
